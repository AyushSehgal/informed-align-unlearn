defaults:
  - task: unlearning_atu
  - hydra: default
  # Allow the model to overwrite the settings below
  - _self_
  - pre_trained_llm: phi-3.5-mini-instruct
  - experiment: ~

unlearning_target: "1_Stephen_King"

# Optional chaining: run multiple (target, layer) unlearning steps sequentially,
# passing the modified model from each step into the next. When set, this takes
# precedence over unlearning_target.
# Example:
unlearning_chain:
  - target: "1_Stephen_King"
    layer: 29
  - target: "106_LeBron_James"
    layer: 21

skip_initial_eval: false

seed: "257640305935986723276204284023905265734"

name: unlearning-${unlearning_target}
exp: atu-unlearning

wandb:
  id: ~
  entity: "ayushseh-carnegie-mellon-university"
  project: "idl-project-unlearning"
  group: ~
  name: atu-layer${task.training_module.pretrained_model_hook_layer}-${unlearning_target}-phi3.5-mini
  mode: ~

hydra:
  job:
    name: ${exp}

trainer:
  accelerator: auto
  devices: auto
  max_epochs: -1
  log_every_n_steps: 10

early_stopping: ~

matmul_precision: medium

